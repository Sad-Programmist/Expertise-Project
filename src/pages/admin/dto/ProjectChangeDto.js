export default class ProjectChangeDto {
    constructor(id, participants, theme, year) {
      this.id = id;
      this.participants = participants;
      this.theme = theme;
      this.year = year;
    }
  }