export default class CategoryChangeDto {
    constructor(id, number, text, maxsum) {
        this.id = id;
        this.number = number;
        this.text = text;
        this.maxsum = maxsum;
    }
}